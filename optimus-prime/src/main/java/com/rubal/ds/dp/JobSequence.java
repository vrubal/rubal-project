/*
package com.rubal.ds.dp;

import java.util.Arrays;

*/
/**
 * @author vrubal
 * @created
 * @project rubal-project
 *//*

public class JobSequence {
    public static void main(String[] args) {
        boolean[] slot = new boolean[n+1];
        Arrays.sort(arr,(Job j1, Job j2)->(j2.profit-j1.profit));

        int ans = 0;
        int jobcount = 0;
        for(Job job : arr){
            int deadline = job.deadline;
            System.out.println(job.profit + " "+job.deadline);
            while(deadline>=1){
                if(slot[deadline]==false){
                    slot[deadline]=true;
                    ans+=job.profit;
                    jobcount++;
                    break;
                }
                deadline--;
            }
        }
        return new int[]{jobcount,ans};
    }
}
*/
